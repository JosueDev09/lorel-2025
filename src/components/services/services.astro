---
// Categorías de servicios
const serviceCategories = [
  {
    title: 'Imprenta Digital y Offset',
    icon: '🖨️',
    color: 'navy',
    services: [
      'Tarjetas de Presentación',
      'Formatos con papel autocopiante',
      'Notas de Remisión',
      'Gafetes',
      'Hojas Membretadas',
      'Blíster',
      'Libretas',
      'Agendas Personalizadas',
      'Volantes',
      'Folletos',
      'Trípticos',
      'Sellos de Goma',
      'Sellos de Seguridad',
      'Recibos de Nómina'
    ]
  },
  {
    title: 'Plotter y Acabados',
    icon: '✂️',
    color: 'wine',
    services: [
      'Lonas',
      'Vinil Impreso',
      'Vinil sobre Trovisel',
      'Vinil sobre Lámina',
      'Vinil de Recorte',
      'Laminado',
      'Encuadernación',
      'Corte',
      'Troquelado',
      'Doblez',
      'Barniz UV'
    ]
  }
];
---

<section class="services" id="servicios">
  <div class="services-container">
    <!-- Header -->
    <div class="services-header">
      <span class="section-label">Nuestros Servicios</span>
      <h2 class="section-title">
        Soluciones <span class="highlight">Personalizadas</span>
      </h2>
      <p class="section-description">
        Contamos con un amplio catálogo de medidas, de las cuales nos ajustamos 
        a sus requerimientos.
      </p>
    </div>

    <!-- Categorías de servicios -->
    <div class="services-grid">
      {serviceCategories.map((category, index) => (
        <div class={`service-category ${category.color}`} data-aos="fade-up" data-aos-delay={index * 100}>
          <div class="category-header">
            <div class="category-icon">{category.icon}</div>
            <h3 class="category-title">{category.title}</h3>
          </div>
          
          <ul class="services-list">
            {category.services.map((service) => (
              <li class="service-item">
                <svg class="check-icon" width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="3">
                  <polyline points="20 6 9 17 4 12"></polyline>
                </svg>
                <span>{service}</span>
              </li>
            ))}
          </ul>

          <div class="category-footer">
            <a href="#contacto" class="service-btn">
              Solicitar información
              <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
                <line x1="5" y1="12" x2="19" y2="12"></line>
                <polyline points="12 5 19 12 12 19"></polyline>
              </svg>
            </a>
          </div>
        </div>
      ))}
    </div>

    <!-- Características adicionales -->
    <div class="features-row">
      <div class="feature-box">
        <div class="feature-icon">📏</div>
        <div class="feature-content">
          <h4 class="feature-title">Medidas Personalizadas</h4>
          <p class="feature-text">Adaptamos nuestros productos a tus necesidades específicas</p>
        </div>
      </div>

      <div class="feature-box">
        <div class="feature-icon">⚡</div>
        <div class="feature-content">
          <h4 class="feature-title">Entrega Rápida</h4>
          <p class="feature-text">Cumplimiento en tiempo y forma de todos los pedidos</p>
        </div>
      </div>

      <div class="feature-box">
        <div class="feature-icon">💎</div>
        <div class="feature-content">
          <h4 class="feature-title">Alta Calidad</h4>
          <p class="feature-text">Materiales premium y acabados profesionales</p>
        </div>
      </div>

      <div class="feature-box">
        <div class="feature-icon">🎯</div>
        <div class="feature-content">
          <h4 class="feature-title">Asesoría Experta</h4>
          <p class="feature-text">Te ayudamos a elegir la mejor opción para tu proyecto</p>
        </div>
      </div>
    </div>
  </div>
</section>

<style>
  .services {
    padding: 6rem 2rem;
    background: linear-gradient(180deg, #ffffff 0%, #f8f9fa 100%);
    position: relative;
  }

  .services-container {
    max-width: 1200px;
    margin: 0 auto;
  }

  /* Header */
  .services-header {
    text-align: center;
    margin-bottom: 4rem;
  }

  .section-label {
    display: inline-block;
    color: var(--color-wine);
    font-weight: 600;
    font-size: 0.875rem;
    text-transform: uppercase;
    letter-spacing: 2px;
    margin-bottom: 1rem;
  }

  .section-title {
    font-size: clamp(2.5rem, 4vw, 3.5rem);
    font-weight: 700;
    color: var(--color-black);
    line-height: 1.2;
    margin-bottom: 1.5rem;
  }

  .highlight {
    color: var(--color-navy);
    position: relative;
  }

  .highlight::after {
    content: '';
    position: absolute;
    bottom: 0;
    left: 0;
    width: 100%;
    height: 12px;
    background: rgba(31, 60, 104, 0.2);
    z-index: -1;
  }

  .section-description {
    font-size: 1.25rem;
    line-height: 1.8;
    color: #555;
    max-width: 700px;
    margin: 0 auto;
  }

  /* Grid de servicios */
  .services-grid {
    display: grid;
    grid-template-columns: repeat(2, 1fr);
    gap: 2.5rem;
    margin-bottom: 4rem;
  }

  .service-category {
    background: white;
    border-radius: 1.5rem;
    padding: 2.5rem;
    box-shadow: 0 10px 40px rgba(0, 0, 0, 0.08);
    transition: all 0.3s ease;
    border: 3px solid transparent;
    position: relative;
    overflow: hidden;
  }

  .service-category::before {
    content: '';
    position: absolute;
    top: 0;
    left: 0;
    right: 0;
    height: 6px;
    background: linear-gradient(90deg, var(--color-navy) 0%, var(--color-wine) 100%);
    transform: scaleX(0);
    transform-origin: left;
    transition: transform 0.3s ease;
  }

  .service-category:hover {
    transform: translateY(-10px);
    box-shadow: 0 20px 60px rgba(0, 0, 0, 0.12);
  }

  .service-category:hover::before {
    transform: scaleX(1);
  }

  .service-category.navy {
    border-color: rgba(31, 60, 104, 0.1);
  }

  .service-category.wine {
    border-color: rgba(156, 45, 50, 0.1);
  }

  /* Header de categoría */
  .category-header {
    display: flex;
    align-items: center;
    gap: 1.5rem;
    margin-bottom: 2rem;
    padding-bottom: 1.5rem;
    border-bottom: 2px solid #f0f0f0;
  }

  .category-icon {
    font-size: 3rem;
    width: 80px;
    height: 80px;
    display: flex;
    align-items: center;
    justify-content: center;
    background: linear-gradient(135deg, #f8f9fa 0%, #e9ecef 100%);
    border-radius: 1rem;
    flex-shrink: 0;
  }

  .service-category.navy .category-icon {
    background: linear-gradient(135deg, var(--color-navy) 0%, #2a4d7d 100%);
    box-shadow: 0 8px 20px rgba(31, 60, 104, 0.3);
  }

  .service-category.wine .category-icon {
    background: linear-gradient(135deg, var(--color-wine) 0%, #b33b40 100%);
    box-shadow: 0 8px 20px rgba(156, 45, 50, 0.3);
  }

  .category-title {
    font-size: 1.75rem;
    font-weight: 700;
    color: var(--color-black);
    line-height: 1.3;
  }

  /* Lista de servicios */
  .services-list {
    list-style: none;
    padding: 0;
    margin: 0 0 2rem 0;
    display: grid;
    grid-template-columns: repeat(2, 1fr);
    gap: 0.875rem;
  }

  .service-item {
    display: flex;
    align-items: flex-start;
    gap: 0.75rem;
    font-size: 1rem;
    color: #555;
    line-height: 1.6;
  }

  .check-icon {
    flex-shrink: 0;
    margin-top: 2px;
  }

  .service-category.navy .check-icon {
    color: var(--color-navy);
  }

  .service-category.wine .check-icon {
    color: var(--color-wine);
  }

  /* Footer de categoría */
  .category-footer {
    padding-top: 1.5rem;
    border-top: 2px solid #f0f0f0;
  }

  .service-btn {
    display: inline-flex;
    align-items: center;
    gap: 0.5rem;
    padding: 1rem 2rem;
    background: transparent;
    color: var(--color-navy);
    border: 2px solid var(--color-navy);
    border-radius: 0.5rem;
    font-weight: 600;
    text-decoration: none;
    transition: all 0.3s ease;
    font-size: 1rem;
  }

  .service-category.navy .service-btn {
    border-color: var(--color-navy);
    color: var(--color-navy);
  }

  .service-category.navy .service-btn:hover {
    background: var(--color-navy);
    color: white;
  }

  .service-category.wine .service-btn {
    border-color: var(--color-wine);
    color: var(--color-wine);
  }

  .service-category.wine .service-btn:hover {
    background: var(--color-wine);
    color: white;
  }

  .service-btn:hover {
    transform: translateX(5px);
  }

  .service-btn svg {
    transition: transform 0.3s ease;
  }

  .service-btn:hover svg {
    transform: translateX(5px);
  }

  /* Características adicionales */
  .features-row {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
    gap: 2rem;
    padding: 3rem;
    background: linear-gradient(135deg, var(--color-navy) 0%, var(--color-black) 100%);
    border-radius: 1.5rem;
    box-shadow: 0 20px 60px rgba(31, 60, 104, 0.3);
  }

  .feature-box {
    display: flex;
    gap: 1.5rem;
    align-items: flex-start;
  }

  .feature-icon {
    font-size: 2.5rem;
    flex-shrink: 0;
  }

  .feature-content {
    flex: 1;
  }

  .feature-title {
    font-size: 1.125rem;
    font-weight: 700;
    color: white;
    margin-bottom: 0.5rem;
  }

  .feature-text {
    font-size: 0.95rem;
    line-height: 1.6;
    color: rgba(255, 255, 255, 0.8);
    margin: 0;
  }

  /* Responsive */
  @media (max-width: 968px) {
    .services {
      padding: 4rem 1.5rem;
    }

    .services-header {
      margin-bottom: 3rem;
    }

    .section-description {
      font-size: 1.125rem;
    }

    .services-grid {
      grid-template-columns: repeat(2, 1fr);
      gap: 2rem;
    }

    .services-list {
      grid-template-columns: repeat(2, 1fr);
      gap: 0.75rem;
    }

    .features-row {
      grid-template-columns: repeat(2, 1fr);
      gap: 1.5rem;
      padding: 2rem;
    }
  }

  @media (max-width: 640px) {
    .services {
      padding: 3rem 1rem;
    }

    .section-title {
      font-size: 2rem;
    }

    .section-description {
      font-size: 1rem;
    }

    .services-grid {
      grid-template-columns: 1fr;
      gap: 1.5rem;
    }

    .service-category {
      padding: 2rem 1.5rem;
    }

    .category-header {
      flex-direction: column;
      text-align: center;
      gap: 1rem;
    }

    .category-icon {
      width: 70px;
      height: 70px;
      font-size: 2.5rem;
    }

    .category-title {
      font-size: 1.5rem;
    }

    .services-list {
      grid-template-columns: 1fr;
    }

    .service-btn {
      width: 100%;
      justify-content: center;
      padding: 0.875rem 1.5rem;
      font-size: 0.95rem;
    }

    .features-row {
      grid-template-columns: 1fr;
      gap: 1.5rem;
      padding: 1.5rem;
    }

    .feature-box {
      flex-direction: column;
      text-align: center;
      gap: 1rem;
    }
  }
</style>
